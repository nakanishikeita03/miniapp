
    <div class="row">
     <% @posts.each do |post| %>
       <%= render partial: "posts/post", locals: { post: post } %>
   <% end %>
  </div>
  <div class="aa">
   <%= paginate(@posts) %>
   </div>

